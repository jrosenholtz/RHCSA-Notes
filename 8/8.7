8.7 Testing Network connections

ping is used to test connectivity

ping -c 1 to send only one packet. You can define a different number of packets if you like

ping -f sends a huge amount of packets in a short amount of time. Don't do this to external sites

ip addr show shows the current configuration

ip route show shows the current routing table

dig can test DNS nameservers