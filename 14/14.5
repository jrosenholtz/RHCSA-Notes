14.5 Creating MBR Partitions with fdisk

even though parted is the default, fdisk is still a common utility worth knowing

as with parted, use fdisk /dev/yourdevice

if when you are done making changes you get a disk in use error, use the partprobe command