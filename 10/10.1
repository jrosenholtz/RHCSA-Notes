10.1 Understanding RPM packages

The RPM is the foundation of software management on RHEL and centos

RPM is a package fromat to install software, it is also a database of installed packages

this makes it easy to manage and install packages

the package containes files taht are compressed with cpio, metadata, and a list of dependencies

rpm packages may contain scripts as well

to isntall packages, repos are used

individual packages can be used, but it should be avoided because you do not get the benefits of the repo