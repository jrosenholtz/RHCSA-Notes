4.6 Lab

use head and tail to display the fifth line of /etc/passwd

use sed to do the same

use awk in a pipe to filter the last column out of the command ps aux

use grep to show the names of all files in /etc/ that have lines that contain the text root

use grep to shwo all lines from all files in /etc that contain exactly 3 char

use grep to find all files that contain the string alex but not alexander